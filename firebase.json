{
    "firestore": {
        "rules": "firestore.rules"
    },
    "storage": [{
        "bucket": "seah-quality2.firebasestorage.app",
        "rules": "storage.rules"
    }],
    "functions": {
        "source": "functions"
    }
}